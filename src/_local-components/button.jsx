
const CustomButton = ({text}) => {
  return (
    <button className="px-3 py-2 rounded-md border-[2px] bg-primary text-white border-solid text-xs border-primary mx-2">{text}</button>
  )
}

export default CustomButton;
